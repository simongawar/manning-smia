spring:
  application:
    name: config-server
  profiles:
    active:
    - native # <-- ACTIVATING THE NATIVE PROFILE
  cloud:
    config:
      server:
        native:
          # TELLS THE SERVER TO LOOK FOR CONFIG FILES (e.g., licensing-service.yml) 
          # in the 'config' directory on the classpath (e.g., src/main/resources/config)
          search-locations: classpath:/config
        # The 'git' section is now correctly commented out, preventing the Git URI error.
        #git:
        #  uri: https://github.com/simongawar/SFWE-510-Cloud-Native-Project-manning-smia.git
        #  searchPaths: licensingservice
    
server:
  port: 8071
  
encrypt:
  key: fje83Ki8403Iod87dne7Yjsl3THueh48jfuO9j4U2hf64Lo 


#Enable all Spring Boot Actuator endpoints.
management:
  endpoints:
    web:
      exposure:
        include: "*"